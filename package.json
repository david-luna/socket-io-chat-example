{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "type": "module",
  "dependencies": {
    "@opentelemetry/api": "^1.8.0",
    "@opentelemetry/auto-instrumentations-node": "^0.45.0",
    "@socket.io/cluster-adapter": "^0.2.2",
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "sqlite": "^5.0.1",
    "sqlite3": "^5.1.6"
  },
  "license": "MIT",
  "// engines": "min Node.js v20 for the convenience of using '--env-file' opt",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start:old": "node index.js",
    "start": "node --env-file=./config.env -r @opentelemetry/auto-instrumentations-node/register index.js"
  }
}
